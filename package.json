{
  "name": "ai-pulse",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @ai-pulse/server && npm run build -w @ai-pulse/widget",
    "dev": "npm run dev -w @ai-pulse/server",
    "start": "npm run start -w @ai-pulse/server",
    "app": "npm run build && npm run start -w @ai-pulse/widget",
    "widget": "npm run start -w @ai-pulse/widget",
    "dist:linux": "npm run dist:linux -w @ai-pulse/widget",
    "gate": "npm run build && node --check packages/web/app.js && node --check packages/widget/renderer/settings.js",
    "linux:install": "npm run linux:install -w @ai-pulse/widget"
  },
  "version": "1.1.0-rc.1",
  "engines": {
    "node": ">=22.14"
  }
}
